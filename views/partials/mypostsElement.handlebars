<article class='post'>
  <div class='title'>
    <h5>{{title}}</h5>
    <span style='float:right'>Posted by: {{user.username}}</span>
    <br/>
    <span style='float:right'>Posted on: {{format_date created_at}}</span>
    <br/>
    <span style='float:right'>Comments: {{comments.length}}</span>
    <br/>
    <span style='float:right' id="hiddenID" value="{{id}}">{{id}}</span>
  </div>
  <div class='content'>
    <p>{{post_text}}</p>
  </div>
</article>